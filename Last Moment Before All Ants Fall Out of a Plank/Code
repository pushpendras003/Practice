class Solution:
    def getLastMoment(self, n: int, left: List[int], right: List[int]) -> int:
      m1=max(left) if len(left)>0 else 0
      m2=min(right) if len(right)>0 else n
      return max(m1,n-m2)
