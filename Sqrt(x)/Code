class Solution:
    def mySqrt(self, x: int) -> int:
        i=0
        prev=0
        while i*i<=x:
            if i*i==x:
                return i
            prev=i
            i+=1
        return prev
